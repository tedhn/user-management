export interface User {
  id: string;
  name: string;
  email: string;
  avatar: string;
  bio: string;
  phoneNumber: string;
  role: string;
  active: boolean;
  createdAt: string; // ISO date string
}
